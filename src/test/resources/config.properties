# Configuration variables for FSeleniumIA framework

# Browser to run tests on: chrome, firefox, edge
browser=chrome

# Target base URL for the tests
baseUrl=https://www.freerangetesters.com

# Default explicit wait timeout in seconds
timeout=10
